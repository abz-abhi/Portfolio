const Softwares = ({imgUrl}) => {
  return (
    <div class="h-10 w-10 rounded-full overflow-hidden bg-[#251C31] flex items-center justify-center">
      <img src={imgUrl} alt="" />
    </div>
  );
};
export default Softwares;
